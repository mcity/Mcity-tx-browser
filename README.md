# Mcity-tx-browser

File browser vue component for transmission.um.city. Can be used standalone when used with the given props as follows:
```vue
<TxBrowser setShare='DATA' standAlone/>
```

Your project must be using vuetify v2 for the browser to display properly.

